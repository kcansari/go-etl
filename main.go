package main

func main() {
	// Create resolvers
	// userResolver := NewUserResolver()
	// postResolver := NewPostResolver()

	// Initialize the service
	// service, err := kg.NewKnowledgeGraphService(
	// 	"MyKnowledgeGraph",
	// 	"./output",
	// 	userResolver,
	// 	postResolver,
	// )
	// if err != nil {
	// 	log.Fatalf("Failed to create service: %v", err)
	// }

	// // Create and run the CSV generator
	// generator := kg.NewCsvGenerator(service)
	// if err := generator.GenerateFiles(context.Background()); err != nil {
	// 	log.Fatalf("Failed to generate CSV files: %v", err)
	// }
}
